<div data-controller='start-survey'>
  <template data-start-survey-target='template_message'>
    <div class='message user-message'>
      <p> Start ⚡️</p>
    </div>
  </template>

  <div class="message <%=message.sender_type%>-message" data-start-survey-target='intro_message'>
    <h1>Hello and welcome 👋 !</h1>
    <P>This demo is a very short survey made for testing the MarketBot chat experience.</P>
    <p><%= message.content %></p>
  </div>

  <%= button_to "Start", questionnaire_completions_path(
    questionnaire_id: chatroom.questionnaire,
                          chatroom_id: chatroom,
                          bot_id: bot
                        ),
                        remote: true,
                        data: { action: "click->start-survey#start", 'start-survey-target': 'start_button' }
  %>
</div>

